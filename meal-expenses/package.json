{
  "name": "meal-expenses",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files 'index.html, style.css, script.js, meal-expenses.json' --no-online --no-notify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^1.8.2",
    "d3": "^3.5.3"
  }
}
